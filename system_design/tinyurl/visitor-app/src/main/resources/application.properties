spring.datasource.hikari.connectionTimeout=300
spring.datasource.hikari.maximumPoolSize=5
spring.datasource.url=${VISITOR_APP_POSTGRES_URL}
spring.datasource.username=${VISITOR_APP_POSTGRES_USER}
spring.datasource.password=${VISITOR_APP_POSTGRES_PASSWORD}

management.health.probes.enabled=true
management.endpoint.health.group.readiness.include=db
